{
  "name": "pallasite-controller-ws",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tiny WebSocket pass-through relay that pairs phone controllers with big-screen game hosts by sessionId. Stand-in for the higher-latency Nostr publish/subscribe path so joystick input feels native.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
